package com.frankwylie;

public class Main {

    public static void main(String[] args) {
	// Java programming language has 50 reserved words.
        // programmers can not use them as names.

        // you can not do this:
        // int int = 5; BUT you can use int int2 = 5;

        // Expressions = the building blocks of all programs.
        // a mile is equal to 1.609344 kilometers.
        double kilometers = (100 * 1.609344); // the expression if the (100 * 1.609344)
        int highScore = 50;

        if (highScore == 50){ // this is a valid expression in the brackets.
            System.out.println("This is an expression");  //  this is also an expression.
        }

        //Lecture Challenge:
        // ------------------------------
        // In the following code that I will type below, write down what parts of the code are expressions.
        int score = 100; // score = 100 is the expression
        if (score > 99){  // The part inside the expression,  the if, brackets are not part of the expression.
            System.out.println("You go the high score!");  // The information inside the brackets is a valid expression.
            score = 0; // This is a valid expression
        }










    }
}
